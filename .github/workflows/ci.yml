name: 🌸 Build & Deploy - Mathilde Fleurs

on:
  push:
    branches: [ main ]
  workflow_dispatch:

# Concurrence - annuler les workflows précédents
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '18'

jobs:
  # 🏗️ Build et Deploy
  build-and-deploy:
    name: 🏗️ Build & Deploy
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📥 Install dependencies
        run: npm ci --prefer-offline --no-audit

      - name: 🏗️ Build
        run: npm run build
        env:
          NODE_ENV: production

      - name: ✅ Success
        run: |
          echo "## 🎉 Build Success!" >> $GITHUB_STEP_SUMMARY
          echo "✅ L'application a été buildée avec succès !" >> $GITHUB_STEP_SUMMARY
          echo "🚀 Vercel va maintenant déployer automatiquement !" >> $GITHUB_STEP_SUMMARY
